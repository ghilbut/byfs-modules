apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "extension.fullname" . }}-test-connection"
  labels:
    {{- include "extension.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "extension.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
