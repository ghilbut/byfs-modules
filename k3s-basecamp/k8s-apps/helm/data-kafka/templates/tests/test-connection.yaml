apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "data-kafka.fullname" . }}-test-connection"
  labels:
    {{- include "data-kafka.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "data-kafka.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
