apiVersion: v1
kind: Service
metadata:
  name: {{ include "data-kafka.fullname" . }}
  labels:
    {{- include "data-kafka.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "data-kafka.selectorLabels" . | nindent 4 }}
